platform: Splunk
source: okta_okta
description: Text that describe current mapping

log_source:
  source_type: [OktaIM2:*]

default_log_source:
  source_type: OktaIM2:*

field_mapping:
  client.user.id: client.user.id
  source.user.id: source.user.id
  User: User
  alternateId: alternateId
  client.user.full_name: client.user.full_name
  source.user.full_name: source.user.full_name
  related.user: related.user
  client.ip: client.ip
  source.ip: source.ip
  user_agent.original: user_agent.original
  userAgent.os: userAgent.os
  userAgent.browser: userAgent.browser
  client.zone: client.zone
  client.device: client.device
  client.id: client.id
  event.action: event.action
  outcome.reason: outcome.reason
  event.outcome: event.outcome
  client.as.number: client.as.number
  client.as.organization.name: client.as.organization.name
  client.domain: client.domain
  source.domain: source.domain
