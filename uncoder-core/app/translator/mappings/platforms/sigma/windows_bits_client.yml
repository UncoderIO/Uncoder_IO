platform: Sigma
source: windows_bits_client


log_source:
  product: [windows]
  category: [bits-client]

default_log_source:
  product: windows
  category: bits-client

field_mapping:
  LocalName: LocalName
  EventID: EventID
  RemoteName: RemoteName
  processPath: processPath